//Instruction :

Clone the project

//How to Run the project 

Add extension in vscode - live server

Run the HTML file which is weather_report.html
Inside HTML, a javascript file is added here, which is weather_report.js 

The server will run with the default port 
Default port - 5500 
Enter the city name then get a result - Show the 5-day weather forecast for a given city.
